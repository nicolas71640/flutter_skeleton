For the moment, the backend unit test works with a real instance of mongo.
Therefore, in order to run the test, you need to first start a mongo instance. 
  mongod --fork --logpath /var/log/mongodb/mongod.log